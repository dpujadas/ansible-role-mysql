---
# tasks file for mysql

- include: 'percona.yml'
  when: mysql_use_percona
  tags:
    - mysql

- include: '{{ item }}.yml'
  with_items:
    - 'requirements'
    - 'install'
    - 'configure'
    - '{{ mysql_init_system }}'
    - 'secure'
    - 'databases-users'
  tags:
    - mysql
